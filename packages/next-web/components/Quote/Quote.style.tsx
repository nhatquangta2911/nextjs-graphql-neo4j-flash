import styled from "styled-components";
import { Row } from "styled/global.style";

export const QuoteWrapper = styled(Row)`
  flex-basis: 50%;
  justify-content: flex-start;
  font-size: 0.9em;
  font-style: italic;
`;
